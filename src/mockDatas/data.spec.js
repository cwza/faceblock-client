import * as data from './data'
describe.skip('', () => {
  it('', () => {
    console.log('posts: ', data.posts);
    console.log('postsItems: ', data.postsItems);
    console.log('users: ', data.users);
    console.log('usersItems: ', data.usersItems);
  });
});
