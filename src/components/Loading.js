import React, { Component } from 'react'

class Loading extends Component {
  render() {
    return (
      <span>
        Now Loading....
      </span>
    )
  }
}

Loading.propTypes = {
}

export default Loading;
